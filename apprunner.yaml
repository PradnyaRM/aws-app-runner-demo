version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "No dependencies to install"
run:
  command: python3 app.py
  network:
    port: 8080
    env: APP_PORT
  env:
    - name: ENVIRONMENT
      value: production
